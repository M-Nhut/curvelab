(function(){'use strict';function setup(){const button=document.getElementById('png')||document.getElementById('lab-png');if(!button)return;button.textContent='Lưu ảnh';const full=button.onclick,menu=document.createElement('div');menu.className='image-export-menu';menu.hidden=true;menu.setAttribute('role','group');menu.setAttribute('aria-label','Định dạng ảnh');const choices=[['PNG đầy đủ · kèm nền',()=>(button.fullExport||full)?.()],['Chỉ đối tượng · PNG trong suốt',()=>button.objectExport?.()]];for(const [label,action]of choices){const b=document.createElement('button');b.type='button';b.textContent=label;b.onclick=()=>{menu.hidden=true;button.setAttribute('aria-expanded','false');action();};menu.append(b);}button.after(menu);button.setAttribute('aria-expanded','false');button.onclick=()=>{menu.hidden=!menu.hidden;button.setAttribute('aria-expanded',String(!menu.hidden));};document.addEventListener('keydown',e=>{if(e.key==='Escape'){menu.hidden=true;button.setAttribute('aria-expanded','false');}});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup);else setup();})();
